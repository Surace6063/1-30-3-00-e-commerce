// ✅ Validation Schema
const checkoutSchema = yup.object().shape({
  payment: yup
    .string()
    .oneOf(["cod", "e-sewa"], "Invalid payment method")
    .required("Payment method is required"),

  full_name: yup
    .string()
    .required("Full name is required")
    .min(3, "Full name must be at least 3 characters"),

  phone: yup
    .string()
    .required("Phone number is required")
    .matches(/^[0-9]{10}$/, "Phone number must be 10 digits"),

  shipping_address: yup
    .string()
    .required("Shipping address is required")
    .min(5, "Address must be at least 5 characters"),
});





// chechout form
const CheckOut = () => {
  // Example dummy cart items — replace with your real cart state
  const cartItems = [
    { id: 1, name: "Product A", quantity: 2, price: 500 },
    { id: 2, name: "Product B", quantity: 1, price: 800 },
  ];

  const total = cartItems.reduce(
    (sum, item) => sum + item.price * item.quantity,
    0
  );

  return (
    <div className="min-h-[90vh] flex justify-center items-center p-8 bg-gray-50">
      <div className="grid md:grid-cols-2 gap-8 max-w-5xl w-full">
        {/* 🛒 Left: Cart Summary */}
        <div className="bg-white border border-slate-200 rounded-lg p-6 h-fit">
          <h2 className="text-xl font-semibold mb-4 text-slate-800">
            Your Cart
          </h2>

          {cartItems.length === 0 ? (
            <p className="text-slate-500">Your cart is empty.</p>
          ) : (
            <ul className="divide-y divide-slate-200">
              {cartItems.map((item) => (
                <li
                  key={item.id}
                  className="py-3 flex justify-between items-center"
                >
                  <div>
                    <h3 className="font-medium text-slate-700">{item.name}</h3>
                    <p className="text-sm text-slate-500">
                      Qty: {item.quantity}
                    </p>
                  </div>
                  <p className="font-semibold text-slate-700">
                    Rs. {item.price * item.quantity}
                  </p>
                </li>
              ))}
            </ul>
          )}

          <div className="border-t border-slate-200 mt-4 pt-4 flex justify-between text-lg font-semibold">
            <span>Total:</span>
            <span>Rs. {total}</span>
          </div>
        </div>

        {/* 🧾 Right: Checkout Form */}
        <div className="bg-white border border-slate-200 rounded-lg p-6">
          <h2 className="text-2xl font-semibold mb-4 text-slate-800">
            Checkout Form
          </h2>

          <form className="space-y-4">
            {/* Payment method */}
            <div>
              <label htmlFor="method" className="block font-medium mb-1">
                Payment Method
              </label>
              <select
                id="method"
                className="w-full border rounded p-2 focus:ring focus:ring-blue-200"
              >
                <option value="cod">Cash on Delivery</option>
                <option value="e-sewa">E-Sewa</option>
              </select>
            </div>

            {/* Full name */}
            <div>
              <label htmlFor="full-name" className="block font-medium mb-1">
                Full Name
              </label>
              <input
                id="full-name"
                type="text"
                placeholder="Enter your name"
                className="w-full border rounded p-2 focus:ring focus:ring-blue-200"
              />
            </div>

            {/* Phone number */}
            <div>
              <label htmlFor="phone" className="block font-medium mb-1">
                Phone Number
              </label>
              <input
                id="phone"
                type="text"
                placeholder="Enter your phone number"
                className="w-full border rounded p-2 focus:ring focus:ring-blue-200"
              />
            </div>

            {/* Shipping address */}
            <div>
              <label htmlFor="address" className="block font-medium mb-1">
                Shipping Address
              </label>
              <input
                id="address"
                type="text"
                placeholder="Enter your shipping address"
                className="w-full border rounded p-2 focus:ring focus:ring-blue-200"
              />
            </div>

            <button
              type="submit"
              className="btn btn-primary w-full"
            >
              Pay
            </button>
          </form>
        </div>
      </div>
    </div>
  );
};

export default CheckOut;




// order table list
import React, { useState } from "react";

const dummyOrders = [
  {
    id: 4,
    full_name: "Surace Thapa",
    phone: "9812345678",
    shipping_address: "Kathmandu",
    payment: "COD",
    status: "Pending",
    total_price: "199.00",
    created_at: "2025-11-10T11:04:36.411419Z",
    items: [
      { id: 5, product_name: "Modern Elegance Teal Armchair", quantity: 2, price: "130.00" },
      { id: 6, product_name: "Wireless Gaming Mouse", quantity: 1, price: "69.00" },
    ],
  },
  {
    id: 3,
    full_name: "Surace Thapa",
    phone: "9812345678",
    shipping_address: "Kathmandu",
    payment: "COD",
    status: "Pending",
    total_price: "89.00",
    created_at: "2025-11-10T09:15:36.440266Z",
    items: [
      { id: 3, product_name: "Classic White Tee - Timeless Style and Comfort", quantity: 2, price: "20.00" },
      { id: 4, product_name: "Wireless Gaming Mouse", quantity: 1, price: "69.00" },
    ],
  },
  {
    id: 2,
    full_name: "Surace Thapa",
    phone: "9812345678",
    shipping_address: "Kathmandu",
    payment: "COD",
    status: "Pending",
    total_price: "0.00",
    created_at: "2025-11-10T09:14:01.539033Z",
    items: [],
  },
];

const CheckOut = () => {
  const [expandedOrder, setExpandedOrder] = useState(null);

  const toggleExpand = (orderId) => {
    setExpandedOrder(expandedOrder === orderId ? null : orderId);
  };

  return (
    <div className="p-8 min-h-screen bg-gray-50">
      <h1 className="text-2xl font-semibold mb-6 text-gray-800">Order List</h1>

      <div className="overflow-x-auto bg-white rounded-xl shadow">
        <table className="min-w-full border-collapse">
          <thead className="bg-gray-100">
            <tr>
              <th className="py-3 px-4 text-left text-sm font-medium text-gray-600">#ID</th>
              <th className="py-3 px-4 text-left text-sm font-medium text-gray-600">Customer</th>
              <th className="py-3 px-4 text-left text-sm font-medium text-gray-600">Phone</th>
              <th className="py-3 px-4 text-left text-sm font-medium text-gray-600">Payment</th>
              <th className="py-3 px-4 text-left text-sm font-medium text-gray-600">Status</th>
              <th className="py-3 px-4 text-left text-sm font-medium text-gray-600">Total ($)</th>
              <th className="py-3 px-4 text-left text-sm font-medium text-gray-600">Date</th>
              <th className="py-3 px-4 text-center text-sm font-medium text-gray-600">Actions</th>
            </tr>
          </thead>

          <tbody>
            {dummyOrders.map((order) => (
              <React.Fragment key={order.id}>
                <tr className="border-t hover:bg-gray-50">
                  <td className="py-3 px-4">{order.id}</td>
                  <td className="py-3 px-4">{order.full_name}</td>
                  <td className="py-3 px-4">{order.phone}</td>
                  <td className="py-3 px-4 capitalize">{order.payment}</td>
                  <td className="py-3 px-4">
                    <span className="bg-yellow-100 text-yellow-800 px-2 py-1 rounded text-xs font-semibold">
                      {order.status}
                    </span>
                  </td>
                  <td className="py-3 px-4 font-medium">${order.total_price}</td>
                  <td className="py-3 px-4 text-sm text-gray-600">
                    {new Date(order.created_at).toLocaleDateString()}
                  </td>
                  <td className="py-3 px-4 text-center">
                    <button
                      onClick={() => toggleExpand(order.id)}
                      className="text-blue-600 hover:underline text-sm"
                    >
                      {expandedOrder === order.id ? "Hide Items" : "View Items"}
                    </button>
                  </td>
                </tr>

                {expandedOrder === order.id && (
                  <tr className="bg-gray-50 border-t">
                    <td colSpan={8} className="p-4">
                      {order.items.length > 0 ? (
                        <div>
                          <h3 className="font-semibold text-gray-700 mb-2">Order Items</h3>
                          <ul className="space-y-2">
                            {order.items.map((item) => (
                              <li
                                key={item.id}
                                className="flex justify-between border rounded-lg p-3 bg-white shadow-sm"
                              >
                                <div>
                                  <p className="font-medium text-gray-800">{item.product_name}</p>
                                  <p className="text-sm text-gray-500">Qty: {item.quantity}</p>
                                </div>
                                <p className="font-semibold text-gray-700">${item.price}</p>
                              </li>
                            ))}
                          </ul>
                        </div>
                      ) : (
                        <p className="text-sm text-gray-500 italic">No items in this order.</p>
                      )}
                    </td>
                  </tr>
                )}
              </React.Fragment>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
};

export default CheckOut;
